### Step 1️⃣

👉 **只做登录接口 + JWT 工具类**

### Step 2️⃣

👉 **加拦截器，但只拦 1–2 个接口测试**

### Step 3️⃣

👉 **把一个 `101L` 改成 `UserContext.getUserId()`**

### Step 4️⃣

👉 **前端接登录页 + token**



1. 你现在的 `GET /playlist/{id}` 可能需要公开（浏览歌单详情），所以我先放行了 `/playlist/*`。
2. 但你“创建歌单 / 加歌到歌单 / 删除歌单”等接口需要登录，它们路径通常是 `/playlist/create`、`/playlist/{id}/music/{mid}`，这些并不会匹配 `"/playlist/*"` 的单星（只匹配一段），**可能会被拦截到**。
    为了不绕晕你，我建议你改成更清晰的放行策略：

- 公开的：只放行你确定公开的 GET
- 需要登录的：默认拦住